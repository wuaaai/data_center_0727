# 多知识库管理 + 批量上传 + 自动分配 — 开发步骤日志

> 创建: 2026-07-27 | 状态: ✅ Phase A-G 核心完成

---

## 完成汇总

### 数据库 (PostgreSQL 10.32.10.161)
- ✅ knowledge_bases: 1行 (默认知识库)
- ✅ region_match_rules: 56条 (12地市 × 4种匹配模式 + 省本级)
- ✅ audit_log: 空表就绪
- ✅ c_metadata: 2092条切片全部迁移, 写入 kb_id='default', enabled=true, tags=[]
- ✅ 索引: idx_c_metadata_kb_id, idx_c_metadata_enabled, idx_kb_is_active

### 后端 API (dcp 8001, 31 端点)
- ✅ KB CRUD: GET/POST /knowledge-bases, GET/PUT/DELETE /{kb_id}, GET /stats
- ✅ 文档管理: GET /knowledge?kb_id=&enabled=, PUT toggle, PUT toggle-batch, GET/PUT metadata
- ✅ 审计日志: _audit() 写入 audit_log
- ✅ 配置: PARSE_ENGINE(maas/local), MAAS_PARSE_URL, MAX_CONCURRENT_DOCS 等

### 双引擎解析
- ✅ mineru_app/routes/file_parse.py: POST /file-parse 对齐 Maas 格式 (files + format=markdown)
- ✅ _process_document_background 重构: PARSE_ENGINE 环境变量切换 maas/mineru
- ✅ _submit_to_parser(): 统一 multipart/form-data 提交

### 入库流程
- ✅ rag_ingest.py: CLI 扩展 kb_id + region_code + tags, DELETE 精确匹配 source+kb_id
- ✅ state_manager.py: create_job 默认 kb_id/tags/region_code
- ✅ routes.py: upload(≤20文件) + ingest_job + ingest_batch 全部透传参数

### 自动匹配引擎
- ✅ region_matcher.py: 4级匹配(code > full > short > pinyin > dirname)
- ✅ 9/9 测试全通过 (中文/拼音/编码/简称/目录名)

### 前端
- ✅ KB卡片首页 → 点击进入文档列表 → 统计卡片置底
- ✅ KB选择器 + KB管理弹窗(新建/重命名/启用开关/删除)
- ✅ 文档表格: 启用开关列 + 标签列 + 元数据弹窗
- ✅ Vite build 通过

### 待下一轮 (P1)
- [ ] BatchImportPanel.vue 批量导入面板
- [ ] scan-directory / import-directory API
- [ ] match-rules CRUD 路由
- [ ] SSE 批量进度推送
- [ ] 检索 SQL 双重过滤 (enabled + is_active)

---

# 内网 Maas 同步 markdown 入库适配 — 开发步骤日志

> 创建: 2026-08-01 | 分支: feat/maas-sync-markdown | 备份: effa971

## 背景

实测内网 Maas 接口（`https://910b.hbmaas.com/.../file_parse`）是**同步接口**，一次 POST 直接返回：
```json
{"backend":"hybrid-auto-engine","version":"2.7.1","results":{"文件名":{"md_content":"# 标题\n..."}}}
```

与本地 MinerU 的差异：
- 本地：异步（提交→轮询 task_id→拿 DOCX），有 `<--split-->` 分片标记
- 内网：同步（一次拿 markdown），无分片标记（用 `#` 标题分章），图片相对路径 `images/xxx.jpg`

## 步骤

### Step 1: 后端 `_process_document_background` 支持 Maas 同步
- 文件: `ragdata_manager/routes.py`
- 内网模式: `_submit_to_parser` 一次调用后，从 `results.{文件名}.md_content` 提取 markdown
- markdown 存 job 状态（新增 `markdown` 字段），preview 用前5000字符，不轮询直接 completed
- 本地模式: 保持现有轮询逻辑不变
- 验证: 模拟内网返回，检查 job 有 markdown 字段
- 状态: ✅

### Step 2: `rag_ingest.py` 支持纯 markdown 入库
- 文件: `mineru_app/rag_ingest.py`
- 新增 `read_and_process_markdown()`: 按 `# ` 一级标题分片，每章一个切片
- `ingest()` 增加 `markdown_text` 参数: 传了用 markdown，否则走 DOCX
- CLI 第7参数: markdown 文件路径
- 图片: `![](images/xxx.jpg)` 相对路径无法访问，保留纯文本
- 验证: 3切片分片测试通过
- 状态: ✅

### Step 3: 后端 `ingest_job` 支持 Maas markdown 入库
- 文件: `ragdata_manager/routes.py`
- Maas 模式下 job 有 markdown 字段，写入临时文件传给 rag_ingest.py 第7参数
- markdown 模式跳过 DOCX output_path 检查，用临时 md 文件作占位
- 验证: 完整流程 上传→解析→入库，检查向量库
- 状态: ✅

### Step 4: 前端展示兼容
- markdown 预览: 内网走 markdown preview（preview_text 已含 markdown 前5000字符）
- job 列表: 下载按钮在无 output_path（markdown 模式）时隐藏
- list_jobs 接口裁剪 markdown 大字段，保留 has_markdown 标记
- 状态: ✅
