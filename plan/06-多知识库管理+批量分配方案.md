# 多知识库管理 & 批量上传 & 自动分配 — 完整设计方案

> 版本: v2.1 | 日期: 2026-07-27
> 修订:
>   - v2.0: 吸收审查报告 6 严重问题 + 6 遗漏
>   - v2.1: 本地 MinerU 接口改造为 `/file_parse` 格式，与内网 Maas 接口统一
>
> 关联: `plan/00-实施计划_P0.md` | `plan/04-05-合并方案_批量上传+区域分配.md` | `plan/06-审查报告_差距分析.md`

---

## 一、解析引擎架构（Maas 主力 + 本地备选，接口统一）

### 1.1 核心决策

内网 Maas 接口格式:
```bash
curl -X POST "https://910b.hbmaas.com/idp-model-1000000232-9093/file_parse" \
  -H "Content-Type: multipart/form-data" \
  -F "files=@./xxx.pdf" \
  -F "format=markdown"
```

**本地 MinerU 也提供完全相同的接口格式**，方便开发调试和未来迁移。切换只需改环境变量。

### 1.2 双引擎统一接口

```
                    POST /file-parse  ← 格式统一
                    (mineru_app 端口 8003)
                          │
            ┌─────────────┴─────────────┐
            │  PARSE_ENGINE=local|maas  │
            └─────────────┬─────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                                    ▼
 ┌──────────────────┐              ┌──────────────────┐
 │ 本地 MinerU       │              │ Maas 内网引擎     │
 │ (同进程异步处理)   │              │ (HTTP 转发调用)   │
 └──────────────────┘              └──────────────────┘

   dcp(8001) 也是调用 8003 的同个 /file_parse 接口
   这样 dcp 不需要关心到底是谁在解析
```

### 1.3 环境变量

```bash
# 解析引擎选择: local | maas
PARSE_ENGINE=local     # 开发环境

# 当 PARSE_ENGINE=maas 时:
MAAS_PARSE_URL=https://910b.hbmaas.com/idp-model-1000000232-9093/file_parse
MAAS_PARSE_TIMEOUT=600

# 当 PARSE_ENGINE=local 时 (无额外配置，直接用本进程内模型)
MINERU_WORKERS=2
```

---

## 二、本地 MinerU 接口改造方案

### 2.1 现状 vs 目标

| | 现状 (parse.py) | 目标 |
|---|----------------|------|
| 路由 | `POST /api/parse` | **`POST /file_parse`** |
| 文件参数 | `file` (单数 UploadFile) | **`files` (复数，支持单个或多个)** |
| 格式参数 | 无 | **`format=markdown`** |
| 输出目录 | `output_dir` (Form) | **去掉，由后端自动管理** |
| 返回格式 | `{task_id, status, position}` | **`{code: 200, task_id: "...", status: "queued"}`** |
| 任务状态 | `GET /api/parse/{task_id}` | **`GET /file_parse/{task_id}`** |
| 进度推送 | `GET /api/parse/{task_id}/progress` | **`GET /file_parse/{task_id}/progress`** |

### 2.2 新建 `mineru_app/routes/file_parse.py`（统一入口）

```python
"""
统一文档解析路由 — 接口格式对齐内网 Maas。
POST /file_parse            - 提交解析 (对齐 Maas 格式)
GET  /file_parse/{task_id}  - 查询状态
GET  /file_parse/{task_id}/progress - SSE 进度
GET  /file_parse/queue      - 队列状态
POST /file_parse/{task_id}/cancel - 取消任务
"""
import os
import time
import asyncio
import json
from pathlib import Path

from fastapi import APIRouter, Form, UploadFile, File, Request
from fastapi.responses import JSONResponse, StreamingResponse

from mineru_app.model_manager import model_manager
from mineru_app.config import ALLOWED_EXTENSIONS, MAX_FILE_SIZE_MB, UPLOAD_DIR

router = APIRouter(tags=["file_parse"])

# ── SSE 进度事件流 ──
async def progress_events(task_id: str):
    """Server-Sent Events 流，推送任务实时进度。"""
    yield f"data: {json.dumps({'task_id': task_id, 'stage': 'init', 'message': '已连接'})}\n\n"
    last_progress = ""
    while True:
        task = model_manager.get_task(task_id)
        if not task:
            yield f"event: error\ndata: {json.dumps({'error': '任务不存在'})}\n\n"
            break
        if task.progress != last_progress:
            yield f"data: {json.dumps({'stage': task.status, 'message': task.progress, 'elapsed': task.elapsed})}\n\n"
            last_progress = task.progress
        if task.status in ("completed", "failed"):
            yield f"event: complete\ndata: {json.dumps({'task_id': task_id, 'status': task.status})}\n\n"
            break
        await asyncio.sleep(1)


# ── 核心路由: 对齐 Maas 的 /file_parse ──

@router.post("/file-parse", summary="统一文档解析入口（对齐 Maas 格式）")
async def file_parse(
    files: UploadFile = File(...),
    format: str = Form("markdown"),
):
    """
    提交文档到 MinerU 模型服务解析。

    参数格式对齐内网 Maas:
      -F "files=@./xxx.pdf"
      -F "format=markdown"

    返回格式:
      {"code": 200, "task_id": "...", "status": "queued", "filename": "..."}
    """
    ext = Path(files.filename).suffix.lower()
    if ext not in ALLOWED_EXTENSIONS:
        return JSONResponse(
            {"code": 400, "error": f"不支持的文件格式: {ext}"}, status_code=400
        )

    content = await files.read()
    if len(content) > MAX_FILE_SIZE_MB * 1024 * 1024:
        return JSONResponse(
            {"code": 413, "error": f"文件过大: {len(content)/1024/1024:.1f}MB"},
            status_code=413,
        )

    # 保存临时文件（任务目录自动管理，不需要外部传 output_dir）
    task_dir = UPLOAD_DIR / "parse_tasks" / f"{int(time.time())}_{files.filename}"
    task_dir.mkdir(parents=True, exist_ok=True)
    input_path = task_dir / files.filename
    input_path.write_bytes(content)

    out_dir = task_dir / "output"
    out_dir.mkdir(parents=True, exist_ok=True)

    task_id = model_manager.submit(file_path=input_path, output_dir=out_dir)

    return JSONResponse({
        "code": 200,
        "task_id": task_id,
        "status": "queued",
        "filename": files.filename,
    }, status_code=202)


@router.get("/file-parse/{task_id}", summary="查询任务状态")
async def get_task(task_id: str):
    """查询解析任务的状态、进度和结果。"""
    task = model_manager.get_task(task_id)
    if not task:
        return JSONResponse({"code": 404, "error": "任务不存在"}, status_code=404)
    return {
        "code": 200,
        "task_id": task.task_id,
        "status": task.status,
        "progress": task.progress,
        "progress_pct": task.progress_pct,
        "elapsed": task.elapsed,
        "result": {
            "output_path": task.result_path,
            "chunk_count": task.chunk_count,
        } if task.status == "completed" else None,
        "error": task.error,
    }


@router.get("/file-parse/{task_id}/progress", summary="SSE 实时进度")
async def get_task_progress(task_id: str):
    """通过 Server-Sent Events 推送实时处理进度。"""
    return StreamingResponse(
        progress_events(task_id),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache", "Connection": "keep-alive"},
    )


@router.get("/file-parse/queue", summary="队列状态")
async def get_queue():
    """查看模型服务的任务队列状态。"""
    return model_manager.get_status()


@router.post("/file-parse/{task_id}/cancel", summary="取消任务")
async def cancel_task(task_id: str):
    """取消正在排队或处理中的任务。"""
    ok = model_manager.cancel_task(task_id)
    return {"code": 200, "ok": ok, "task_id": task_id}
```

### 2.3 旧路由保留（兼容过渡）

旧的 `routes/parse.py` 路由 **保留不删**（`POST /api/parse` 等），因为 `routes.py` 中的 `_process_document_background` 通过 `MINERU_SERVICE_URL + "/api/parse"` 调用。

改造完成后 dcp 侧逐步切换到 `/file_parse` 格式。

### 2.4 server.py 注册新路由

```python
# server.py 新增一行
from mineru_app.routes import file_parse  # 新增
app.include_router(file_parse.router)
```

### 2.5 dcp(8001) 侧不再直接调 /api/parse，统一走 /file_parse

```python
# ragdata_manager/config.py
# 新增: 本地 MinerU 的 file_parse 入口
MINERU_FILE_PARSE_URL = os.getenv(
    "MINERU_FILE_PARSE_URL",
    "http://127.0.0.1:8003/file-parse"
)
```

---

## 三、文档解析后端选择逻辑

```
当前环境:

  开发 (PARSE_ENGINE=local):
    dcp(8001) → HTTP → mineru(8003) /file-parse → 本地 MinerU 模型

  生产 (PARSE_ENGINE=maas):
    dcp(8001) → HTTP → Maas(内网) /file_parse → Maas 集群

  两种环境 dcp 代码完全一样，只是环境变量不同。
```

dcp 侧代码 (ragdata_manager/routes.py):

```python
# 根据 PARSE_ENGINE 选择解析后端 URL
PARSE_URL = (
    MAAS_PARSE_URL
    if os.getenv("PARSE_ENGINE", "local") == "maas"
    else MINERU_FILE_PARSE_URL
)

def _submit_to_parser(file_path: Path) -> dict:
    """通用文档提交：格式对齐 /file_parse。"""
    import urllib.request, json as _json, uuid

    boundary = "----FormBoundary" + uuid.uuid4().hex[:16]
    file_bytes = file_path.read_bytes()
    file_name = file_path.name.replace('"', "'").replace("\n", " ").replace("\r", " ")

    body_head = (
        f"--{boundary}\r\n"
        f'Content-Disposition: form-data; name="files"; filename="{file_name}"\r\n'
        f"Content-Type: application/octet-stream\r\n\r\n"
    ).encode("utf-8")
    body_tail = (
        f"\r\n--{boundary}\r\n"
        f'Content-Disposition: form-data; name="format"\r\n\r\n'
        f"markdown\r\n"
        f"--{boundary}--\r\n"
    ).encode("utf-8")
    body = body_head + file_bytes + body_tail

    req = urllib.request.Request(
        PARSE_URL,
        data=body,
        headers={"Content-Type": f"multipart/form-data; boundary={boundary}"},
    )
    with urllib.request.urlopen(req, timeout=600) as resp:
        return _json.loads(resp.read().decode("utf-8"))
```

---

## 四、当前代码现状和差距

| 当前能力 | 限制 | 改造成什么 |
|----------|------|-----------|
| 本地 MinerU 接口 | `POST /api/parse` 格式与 Maas 不同 | **新增 `POST /file-parse` 格式对齐 Maas** |
| dcp 调用 MinerU | 硬编码 `/api/parse` | **统一走 `PARSE_URL = maas或mineru的 /file-parse`** |
| 单次上传 | 最多 3 个文件 | 快速入口放宽到 20 个；批量走服务器目录扫描 |
| 知识库 | 不存在，单张表 | 新建 `knowledge_bases` 表 + CRUD + SQLite 降级 |
| 文档启用/禁用 | 不支持 | Switch 切换，检索自动过滤，审计日志 |
| 元数据 | 7 字段，无 kb_id/enabled/tags | 扩展到 10 字段 + 元数据弹窗 |
| 批量上传 | 浏览器 3 个 | 目录扫描导入 + 批量进度面板 |
| 地区分配 | 手动逐个选 | 文件名/目录名自动匹配 + 未匹配池手动分配 |

---

## 五、数据库设计

### 5.1 `knowledge_bases` 表

```sql
CREATE TABLE IF NOT EXISTS knowledge_bases (
    id                 VARCHAR(64) PRIMARY KEY,
    name               VARCHAR(255) NOT NULL,
    description        TEXT DEFAULT '',
    directory_keywords TEXT DEFAULT '[]',
    is_active          BOOLEAN DEFAULT TRUE,
    owner_id           VARCHAR(128) DEFAULT '',
    created_at         TIMESTAMPTZ DEFAULT NOW(),
    updated_at         TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_kb_is_active ON knowledge_bases(is_active);
```

### 5.2 `region_match_rules` 表

```sql
CREATE TABLE IF NOT EXISTS region_match_rules (
    id           SERIAL PRIMARY KEY,
    pattern      VARCHAR(255) NOT NULL,
    region_code  VARCHAR(9) NOT NULL,
    kb_id        VARCHAR(64) DEFAULT '',
    match_type   VARCHAR(32) DEFAULT 'keyword',
    priority     INT DEFAULT 0,
    is_active    BOOLEAN DEFAULT TRUE,
    created_at   TIMESTAMPTZ DEFAULT NOW()
);
```

### 5.3 `audit_log` 表

```sql
CREATE TABLE IF NOT EXISTS audit_log (
    id           SERIAL PRIMARY KEY,
    action       VARCHAR(64) NOT NULL,
    target_type  VARCHAR(32) NOT NULL,
    target_id    VARCHAR(128) NOT NULL,
    detail       TEXT DEFAULT '',
    operator     VARCHAR(128) DEFAULT '',
    created_at   TIMESTAMPTZ DEFAULT NOW()
);
```

### 5.4 `c_metadata` JSONB 扩展

现有不变。新增 `kb_id` (string, "default"), `enabled` (boolean, true), `tags` (jsonb array, []).

### 5.5 SQLite 本地缓存

```sql
-- kb_cache.db
CREATE TABLE IF NOT EXISTS kb_cache (
    id TEXT PRIMARY KEY, name TEXT NOT NULL, is_active INTEGER DEFAULT 1, synced_at REAL DEFAULT 0
);
```

---

## 六、自动匹配引擎

**新建文件:** `data_processing_center/ragdata_manager/region_matcher.py`

### 6.1 匹配优先级

| 优先级 | 方式 | 示例文件名 | 匹配结果 |
|--------|------|-----------|---------|
| 1 | 区划编码 | `1301000_预算报告.pdf` | 石家庄市 |
| 2 | 中文全称 | `石家庄市预算执行报告.docx` | 石家庄市 |
| 3 | 中文简称 | `石市财政2026.pdf` | 石家庄市 |
| 4 | 拼音 | `shijiazhuang_report.pdf` | 石家庄市 |
| 5 | 目录名 | `D:\邯郸市\预算.pdf` | 邯郸市 |

### 6.2 核心接口

```python
class RegionMatcher:
    def match(self, filename: str, dirname: str = "") -> dict:
        """单文件匹配 → {region_code, region_name, confidence, match_type}"""

    def batch_match(self, files: list[dict]) -> dict:
        """批量匹配 → {matched, unmatched, by_region}"""

    def preview_allocation(self, files: list[dict]) -> list:
        """预览分配 → [{filename, region_code, region_name, kb_id, confidence}]"""
```

---

## 七、批量处理（目录扫描为主）

### 7.1 可行性判断

浏览器上传 500 个文件不可行。正确路线：

```
少量 (≤20个): 浏览器 <input multiple> → POST /ragdata/upload
大量 (20-500个): 用户放文件到服务器目录 → scan-directory → import-directory
```

### 7.2 API

| 方法 | URL | 说明 |
|------|-----|------|
| `POST` | `/ragdata/file-parse/scan-directory` | 扫描服务器目录 → 自动分配预览 |
| `POST` | `/ragdata/file-parse/import-directory` | 确认分配 → 批量创建 job 并处理 |
| `GET` | `/ragdata/file-parse/batch/{batch_id}` | 批量进度 |
| `GET` | `/ragdata/file-parse/batch/{batch_id}/progress` | SSE 实时进度 |
| `POST` | `/ragdata/file-parse/batch/{batch_id}/cancel` | 取消 |
| `POST` | `/ragdata/file-parse/batch/{batch_id}/retry-failed` | 重试失败项 |

### 7.3 并发控制（单层线程池）

```python
# config.py
MAX_CONCURRENT_DOCS = int(os.getenv("MAX_CONCURRENT_DOCS", "6"))
_doc_executor = ThreadPoolExecutor(max_workers=MAX_CONCURRENT_DOCS)
```

去掉了 v1.0 多余的 Semaphore，只用线程池自身做排队。

---

## 八、后端 API 完整设计

### 8.1 知识库 CRUD

| 方法 | URL | 说明 |
|------|-----|------|
| `GET` | `/ragdata/knowledge-bases` | 列出 KB（含 SQLite 降级） |
| `POST` | `/ragdata/knowledge-bases` | 创建 KB |
| `GET` | `/ragdata/knowledge-bases/{kb_id}` | KB 详情 + 统计 |
| `PUT` | `/ragdata/knowledge-bases/{kb_id}` | 更新 KB |
| `DELETE` | `/ragdata/knowledge-bases/{kb_id}` | 删除 KB |
| `GET` | `/ragdata/knowledge-bases/stats` | 聚合统计 |

### 8.2 文档管理

| 方法 | URL | 说明 |
|------|-----|------|
| `GET` | `/ragdata/knowledge` | 文档列表（`?kb_id=` + `?enabled=`） |
| `PUT` | `/ragdata/knowledge/{source}/toggle` | 切换启用/禁用 |
| `PUT` | `/ragdata/knowledge/toggle-batch` | 批量切换 |
| `GET` | `/ragdata/knowledge/{source}/metadata` | 完整元数据 |
| `PUT` | `/ragdata/knowledge/{source}/metadata` | 更新 kb_id / tags |

### 8.3 匹配规则

| 方法 | URL | 说明 |
|------|-----|------|
| `GET` | `/ragdata/region-match-rules` | 规则列表 |
| `POST` | `/ragdata/region-match-rules` | 新增规则 |
| `DELETE` | `/ragdata/region-match-rules/{rule_id}` | 删除规则 |

### 8.4 保留现有路由

现有路由全部保留不动。`upload` 放宽到 20 文件 + 新增 `kb_id`/`auto_allocate` 参数。

---

## 九、检索过滤

Phase E 完成后，检索 SQL 加双重过滤：
```sql
WHERE (c_metadata->>'enabled')::boolean = true
  AND c_metadata->>'kb_id' IN (SELECT id FROM knowledge_bases WHERE is_active = true)
```

---

## 十、前端设计

### 10.1 组件树

```
ragManage.vue (改造)
├── KB 选择器 + KB 管理弹窗
├── 文档表格: 启用开关列 + 标签列 + [元数据]按钮
├── MetadataDialog.vue (新建)
├── [快速上传] (≤20文件, 加 kb_id 选择)
├── [批量导入] → BatchImportPanel.vue (新建)
│   ├── 步骤1: 来源选择 (选择文件 / 扫描目录)
│   ├── 步骤2: 分配预览 (RegionFileGroup × N + UnassignedFilePool)
│   ├── 步骤3: 确认提交
│   └── 步骤4: SSE 实时进度
```

---

## 十一、入库流程改造

### `rag_ingest.py`
- CLI: `python rag_ingest.py <file_path> <source_id> [kb_id] [region_code] [tags_json]`
- metadata 新增 `kb_id`, `enabled`, `tags`
- DELETE 旧切片改为精确匹配 `source + kb_id`

### `state_manager.py`
- create_job 默认 `kb_id: "default"`, `tags: []`

### `routes.py`
- 两个 subprocess.run 调用点都传 kb_id + region_code + tags
- ingest_job() 读取 job 中的 kb_id 传递给子进程

---

## 十二、向下兼容

| 场景 | 策略 |
|------|------|
| 旧 `/api/parse` 路由 | 保留不删，dcp 逐步迁移到 `/file-parse` |
| 原有3文件上传 | 保留为快速入口，放宽到 20，加 kb_id |
| 不传 kb_id | 默认 `"default"` |
| c_metadata 无新字段 | 迁移脚本 + 代码 fallback |

---

## 十三、实施顺序

```
Phase 0: 前置验证 (0.5天)  ← 必须先做
  P0.1: Maas 接口可达性 + 返回格式确认
  P0.2: Embedding 批量能力探测
  P0.3: pgvector COPY 验证

Phase A: 数据库 (0.5天)
  A1: 迁移脚本 + 回滚脚本
  A2: 执行迁移 + 插入完整12地市默认规则

Phase B: 本地 MinerU 接口改造 (0.5天)  ← 新增
  B1: 新建 mineru_app/routes/file_parse.py
  B2: server.py 注册新路由
  B3: config.py 加 MINERU_FILE_PARSE_URL + PARSE_ENGINE

Phase C: 入库改造 (0.5天)
  C1: config.py 常量
  C2: rag_ingest.py CLI + metadata 扩展 + DELETE 精确匹配
  C3: state_manager.py 默认 kb_id
  C4: routes.py upload/ingest/ingest_batch 透传

Phase D: dcp 解析适配 (0.5天)
  D1: _submit_to_parser() 统一走 /file-parse 格式
  D2: _process_document_background 适配双引擎

Phase E: 自动匹配引擎 (1天)
  E1: region_matcher.py
  E2: preview-allocation + scan-directory + import-directory
  E3: match-rules CRUD

Phase F: 知识库管理后端 (1天)
  F1: KB CRUD (含 SQLite 降级)
  F2: knowledge 过滤扩展
  F3: toggle + metadata (含审计日志)
  F4: stats + 检索过滤

Phase G: 前端 (2天)
  G1: dataManage.js
  G2: KB 选择器+管理弹窗
  G3: 启用开关+标签列+MetadataDialog
  G4: BatchImportPanel + RegionFileGroup + UnassignedFilePool

Phase H: 验证 (0.5天)
```

**依赖链:** Phase 0 → A → B → C → D → E/F (可并行) → G → H

---

## 十四、关键文件清单

| 文件 | 变更 | 内容 |
|------|------|------|
| `mineru_app/routes/file_parse.py` | **新建** | `/file_parse` 统一入口，格式对齐 Maas |
| `mineru_app/server.py` | 修改 | 注册 file_parse 路由 |
| `ragdata_manager/routes.py` | **重改** | dcp 统一走 `/file-parse`，目录扫描，批量导入，KB CRUD，toggle，metadata |
| `ragdata_manager/region_matcher.py` | **新建** | 地区自动匹配引擎 |
| `ragdata_manager/batch_tracker.py` | **新建** | 批量任务追踪 + SSE |
| `ragdata_manager/kb_cache.py` | **新建** | SQLite 缓存降级 |
| `ragdata_manager/audit.py` | **新建** | 审计日志 |
| `ragdata_manager/config.py` | 修改 | PARSE_ENGINE, MAAS_PARSE_URL, MINERU_FILE_PARSE_URL 等 |
| `mineru_app/rag_ingest.py` | 修改 | CLI + metadata + DELETE 精确匹配 |
| `mineru_app/state_manager.py` | 修改 | 默认 kb_id |
| `app/src/views/ragManage.vue` | 修改 | KB选择器、管理弹窗、开关、标签列、批量入口 |
| `app/src/views/BatchImportPanel.vue` | **新建** | 批量导入面板 |
| `app/src/components/RegionFileGroup.vue` | **新建** | 地区文件组 |
| `app/src/components/UnassignedFilePool.vue` | **新建** | 未分配文件池 |
| `app/src/components/MetadataDialog.vue` | **新建** | 元数据弹窗 |
| `app/src/api/dataManage.js` | 修改 | 新增 15+ API |
| `sql/migrate_v2_kb.sql` | **新建** | 迁移脚本 |
| `sql/migrate_v2_kb_rollback.sql` | **新建** | 回滚脚本 |
