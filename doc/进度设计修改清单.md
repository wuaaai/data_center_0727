# 进度设计 — 修改清单

## ✅ 已完成的修改

### 1. `mineru_0720/web_app/model_manager.py` — 环节文案重写

**修改内容：**
- 环节 1 "加载引擎" → "提交任务"
- 环节 2 "解析文档"（保持，"解析"已是最贴合用户心智的词）
- 环节 3 "整理结构" → "整理结果"
- 环节 4 "生成文档"（保持）

```python
_S = [
    ("提交任务", "正在发送文档，准备分析...",  ["排队等待处理", "模型服务就绪", "提交"]),
    ("解析文档", "正在逐页识别文字与表格...",  ["[1/4]", "PDF 解析中", "Layout Predict", "OCR-det", "OCR-rec", "MFR Predict", "Seal Predict", "Table-ocr", "Table-wired", "Table-wireless"]),
    ("整理结果", "正在按章节整理文档结构...",  ["[2/4]", "[3/5]", "[4/5]", "加载内容", "文本分块", "段落生成"]),
    ("生成文档", "正在生成可下载 Word 文件...", ["[5/5]", "生成 DOCX", "Processing pages"]),
]
```

**进度条：**
- 环节 1/3/4（不确定）→ `progress_pct = 0`，前端显示动画条纹
- 环节 2（确定）→ tqdm 实时%，前端显示实心进度条

**子步骤中文：**
- "版面检测"、"OCR文字检测"、"OCR文字识别"、"公式识别"、"印章检测"、"页面合成"
- （表格/OCR 内部术语保留，但只在环节 2 中出现，语义已被"逐页识别"涵盖）

### 2. `data_processing_center/ragdata_manager/routes.py` — 排队阶段加上环节前缀

**修改内容：**
- `"排队等待处理..."` → `"[环节 1/4] 提交任务：正在排队等待处理..."`
- `"等待模型服务就绪..."` → `"[环节 1/4] 提交任务：等待模型服务就绪..."`
- `"正在提交到模型服务..."` → `"[环节 1/4] 提交任务：正在发送文档..."`
- `"已提交到模型服务，等待处理..."` → `"[环节 1/4] 提交任务：已发送，等待引擎处理..."`

### 3. `data_processing_center/app/src/views/ragManage.vue` — 前端进度条改为始终显示

**修改内容：**
- 删除 `v-if="row.progress_pct>0"` 的条件
- `progress_pct > 0` → 实心蓝色 `<el-progress :percentage="row.progress_pct" :stroke-width="6" />`
- `progress_pct == 0` 且状态 processing → 动画条纹 `<el-progress :percentage="50" :indeterminate="true" :stroke-width="6" />`
- completed 且 progress=="处理完成" → 绿色 100% `<el-progress :percentage="100" color="#22c55e" />`
- failed → 红色 0% `<el-progress :percentage="0" color="#ef4444" />`
